// 1. une fonction qui fait l'addition de 5 + x

function add5(x) {
    return 5 + x;
};

// 2. une fonction qui fait l'addition de 10 + x
function add10(x) {
    return 10 + x;
};

// 3.Question Si on voulais encore les autres fonctions comme 15 + x, 20 + x etc, Est-ce qu'on aurait les tous définir.


module.exports = {
    add5,
    add10,
}
